mixin createPostForm(userLoggedIn)
    .postFormContainer
        .userImageContainer
            img(src=userLoggedIn.profilePic, alt="Profile Picture")
            p @#{userLoggedIn.username}
        .textareaContainer
            .card(style='width: 18rem;')
                img.card-img-top(src='/images/waffles.png' alt='Card image cap')
                .card-body
                    h5.card-title Top 5 (Category)
                    p.card-text
                        | A place for a user to put a caption
                ul.list-group.list-group-flush
                    textarea.form-control.z-depth-1(rows='1' placeholder='#1')
                    textarea.form-control.z-depth-1(rows='1' placeholder='#2')
                    textarea.form-control.z-depth-1(rows='1' placeholder='#3')
                    textarea.form-control.z-depth-1(rows='1' placeholder='#4')
                    textarea.form-control.z-depth-1(rows='1' placeholder='#5')
                .card-body
                    a.card-link(href='https://www.youtube.com/watch?v=PjLw1E7tTuc') Link a Video or Whatever
        input.btn.btn-primary(type='submit' value='Submit')
